name: Test Build

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: upload
        uses: ./
        with: 
          host: 'https://lecon.myds.me:5001'
          sharing_id: 'gDVIsFgc6'
          uploader_name: 'test_name'
          password: 'a123456'
          file: 'hello.txt'
